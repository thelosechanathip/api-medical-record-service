const { msg } = require('../../../services/message.service')

// ดึงข้อมูลของตาราง patient_services ทั้งหมด
exports.fetchAllPatientServices = (req, res) => {
    try {
        
    } catch (err) {
        throw new Error(err.message)
    }
}